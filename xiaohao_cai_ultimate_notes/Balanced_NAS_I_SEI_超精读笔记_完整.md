# Balanced NAS I: 特定辐射源识别的神经架构搜索

> **超精读笔记** | 完整PDF分析
> 分析时间：2026-02-21
> 论文来源：IEEE RFID-TA 2022
> DOI: 10.1109/RFID-TA54958.2022.9924146
> 作者：Mingyang Du, Ping Zhong, Xiaohao Cai, Daping Bi, Zhifei Li
> 领域：神经架构搜索、特定辐射源识别、雷达信号处理

---

## 📄 论文元信息

| 属性 | 信息 |
|------|------|
| **标题** | Balanced neural architecture search and optimization for specific emitter identification |
| **作者** | Mingyang Du, Ping Zhong, **Xiaohao Cai**, Daping Bi, Zhifei Li |
| **年份** | 2022 |
| **会议** | IEEE RFID-TA 2022 |
| **页数** | 4页 (220-223) |
| **领域** | 信号处理、深度学习、NAS |
| **任务类型** | 特定辐射源识别(SEI)、神经架构搜索 |

### 📝 摘要翻译

固定结构的神经网络在处理不同分类任务时缺乏灵活性，促使人们对开发自动化神经架构搜索(NAS)方法越来越感兴趣。迄今为止的方法主要考虑NAS搜索结果的分类准确率，但另一个重要因素——计算成本——被忽略了。在本文中，我们建立了一个可行性问题模型，受分类准确率和计算成本两方面特定约束，这可以大大增强针对最近工作中提出的固定"平衡函数"在识别不同电磁环境中雷达信号的灵活性。此外，为了能够遍历约束形成的无限可行区域，我们提出了一种基于高斯过程回归模型的简单有效方法，通过微调初始化平衡函数并利用满足约束的数据分布。实验结果表明，所提出的NAS技术在设计网络结构方面优于手动设计模型，准确率相当，但与传统NAS算法相比计算成本更低。

**关键词**: 特定辐射源识别、时频分布、神经架构搜索、高斯过程

---

## 🎯 一句话总结

用高斯过程回归优化"平衡函数"，实现兼顾准确率和计算成本的神经架构搜索，应用于雷达信号特定辐射源识别。

---

## 🔑 核心创新点

1. **可行性问题建模**：将平衡函数设计建模为约束优化问题
2. **高斯过程回归优化**：用GPR遍历无限可行区域
3. **灵活约束调整**：可根据需求调整准确率/效率权重
4. **计算成本大幅降低**：比标准NAS减少3倍以上参数
5. **SEI应用验证**：在雷达信号数据集上验证有效

---

## 📊 背景与动机

### 特定辐射源识别(SEI)的挑战

| 问题 | 描述 | 后果 |
|------|------|------|
| 固定网络局限 | 单一网络难处理多变SNR | 通用性差 |
| 深度网络过拟合 | 高SNR时深层网络过拟合 | 性能退化 |
| NAS计算昂贵 | 传统NAS需3150 GPU天 | 实用性差 |

### 现有方法的不足

- **准确率优先NAS**：忽视计算成本
- **固定平衡函数**：缺乏灵活性
- **手动设计**：依赖专家经验

---

## 💡 方法详解

### 3.1 Block-Cell结构

```
Block Bb = [I1, I2, O1, O2, C]
- I1, I2: 输入张量
- O1, O2 ∈ O: 操作（卷积、池化等）
- C ∈ C: 组合操作（加法、拼接）
```

Cell由多个Block组成，CNN由多个Cell堆叠而成。

### 3.2 平衡函数定义

**基本性质**：
$$F(x, y_i) > F(x, y_j), \forall y_i < y_j$$ （相同准确率，更简单模型分数更高）

$$F(x_i, y) < F(x_j, y), \forall x_i < x_j$$ （相同效率，更准确模型分数更高）

**分解形式**：
$$F(x, y) = F_1(x)F_2(y) \text{ 或 } F(x, y) = F_1(x) + F_2(y)$$

### 3.3 可行性约束

$$\text{find } F(x, y)$$

$$\text{s.t. } F(x_i, y_i) > F(x_j, y_j), x_i - x_j > 2\lambda$$

$$F(x_i, y_i) < F(x_j, y_j), x_i - x_j < 2\lambda, y_i - y_j > 2\gamma$$

$$F(x_i, y_i) > F(x_j, y_j), x_i - x_j > 0, y_i - y_j < 0$$

### 3.4 高斯过程优化

**后验分布**：
$$p(Y^*|Y) = \mathcal{N}(\mu_{GP}^*, \sigma^2_{GP}^*)$$

**均值和方差**：
$$\mu_{GP}^* = \kappa_f^{*\top}(\kappa_{ff} + \sigma^2 I)^{-1}Y$$

$$\sigma^2_{GP}^* = \sigma^2 + \kappa_{**} - \kappa_f^{*\top}(\kappa_{ff} + \sigma^2 I)^{-1}\kappa_f^*$$

### 3.5 初始化

$$F_1(x) = \frac{1}{1 + e^{-10(x-0.5)}}$$

$$F_2(y) = e^{-y}$$

$$F(x, y) = \frac{1}{2}[F_1(x) + F_2(y)]$$

---

## 📈 实验结果

### 实验设置

| 配置 | 值 |
|------|-----|
| 数据集 | SIGNAL-5 (7类雷达信号，5种SNR) |
| SNR范围 | -10dB, -8dB, -5dB, -1dB, 无噪声 |
| 图像大小 | 64×64 (STFT时频图) |
| LSTM隐藏层 | 100 |
| CNN滤波器 | 32和64，步长2 |
| 阈值 | λ=0.03, γ=0.02 |

### 性能对比 (SNR=-10dB)

| 方法 | 平均准确率 | 平均参数量 |
|------|-----------|-----------|
| **GP-based NAS** | **85.5%** | **2.1×10⁵** |
| 初始化平衡函数 | 82.9% | 1.4×10⁵ |
| 仅准确率NAS | 84.2% | 7.2×10⁵ |

**关键发现**：
- GP方法准确率最高
- 比标准NAS参数量减少**3倍以上**
- 通过调整λ、γ可灵活权衡

---

## 💭 个人评价与启发

### 优点
1. 创新性地用GPR优化平衡函数
2. 显著降低计算成本
3. 灵活可调的约束机制
4. Xiaohao Cai参与，理论与实践结合

### 局限性
1. 仅在雷达信号数据集验证
2. 超参数(λ, γ)需要调优
3. Block数量限制(最多8个)

### 对钢哥哥的启发
1. **NAS思想**：自动搜索最优网络结构
2. **平衡函数**：多目标优化(准确率vs效率)的思路
3. **GPR应用**：高斯过程在优化中的价值
4. **雷达信号**：时频分析+CNN的组合方式

---

## 📚 参考文献

[1] Talbot et al., "Specific emitter identification and verification", 2003
[7] Zoph & Le, "Neural architecture search with reinforcement learning", ICLR 2017
[9] Liu et al., "Progressive neural architecture search", ECCV 2018
[10] Du et al., "Balanced NAS and its application in SEI", IEEE TSP 2021

---

*本笔记基于PDF完整内容生成 | 2026-02-21*
