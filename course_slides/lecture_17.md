# 第十七讲：Neural Varifolds

## Neural Varifolds for Geometric Representation

---

### 📋 本讲大纲

1. 几何表示方法概述
2. Varifold理论基础
3. Neural Varifolds架构
4. 神经核函数
5. 应用与实验

---

### 17.1 几何表示的挑战

#### 传统表示方法

| 方法 | 优点 | 缺点 |
|------|------|------|
| 点云 | 简单 | 无拓扑 |
| 网格 | 显式拓扑 | 需要连接性 |
| 体素 | 规则 | 内存消耗大 |
| 隐式函数 | 连续 | 需要采样 |

#### 统一表示的需求

- 处理不同分辨率的输入
- 支持不完整数据
- 保持几何一致性

---

### 17.2 Varifold理论

#### 定义

Varifold是测度的推广，用于描述几何对象

对于曲面 $S$，定义Varifold：
$$\mu_S(\phi) = \int_S \phi(x, n_x) d\mathcal{H}^2(x)$$

其中 $\phi$ 是测试函数，$n_x$ 是法向量

#### 数学性质

- 紧性：有界Varifold空间紧致
- 弱收敛：$V_n \rightharpoonup V$
- 可用于形状分析

---

### 17.3 Varifold度量

#### 距离定义

两个形状 $S_1, S_2$ 的Varifold距离：
$$d(S_1, S_2)^2 = \|\mu_{S_1} - \mu_{S_2}\|^2$$

#### 核度量

使用核函数 $K$：
$$\|\mu_{S_1} - \mu_{S_2}\|^2 = \|\mu_{S_1}\|^2 + \|\mu_{S_2}\|^2 - 2\langle \mu_{S_1}, \mu_{S_2} \rangle$$

其中：
$$\langle \mu_{S_1}, \mu_{S_2} \rangle = \int_{S_1}\int_{S_2} K(x, n, y, m) dS_x dS_y$$

---

### 17.4 Neural Varifolds架构

#### 核心思想

用神经网络参数化Varifold表示

#### 架构

```
输入：点云 P = {p_i}
     │
     ▼
编码器 (PointNet/PointNet++)
     │
     ▼
特征 f_i ∈ R^d
     │
     ▼
Varifold层：计算核积分
     │
     ▼
Varifold表示 V ∈ H (Hilbert空间)
```

---

### 17.5 神经核函数

#### 设计

$$K_\theta(x, n, y, m) = k_\theta^x(x, y) \cdot k_\theta^n(n, m)$$

其中：
- $k^x$：位置核（如高斯核）
- $k^n$：法向量核（如von Mises-Fisher）
- $\theta$：可学习参数

#### 实现

$$k^x(x, y) = \exp\left(-\frac{\|x - y\|^2}{\sigma_x^2}\right)$$
$$k^n(n, m) = \exp\left(\frac{n \cdot m}{\sigma_n^2}\right)$$

---

### 17.6 Varifold层

#### 前向计算

给定点云 $\{p_i, n_i\}_{i=1}^N$：

$$\mu = \sum_{i=1}^N a_i \delta_{(p_i, n_i)}$$

Varifold内积：
$$\langle \mu_1, \mu_2 \rangle = \sum_{i} \sum_{j} a_i a_j K(p_i, n_i, q_j, m_j)$$

#### 批处理

支持批量处理多个形状

---

### 17.7 应用：形状匹配

#### 问题

给定两个形状 $S_1, S_2$，求最优对应

#### 方法

1. 编码为Varifold表示
2. 最小化Varifold距离
3. 学习变形场

#### 优势

- 处理不完整数据
- 对噪声鲁棒
- 可微分，端到端训练

---

### 17.8 应用：形状补全

#### 问题

从不完整观测恢复完整形状

#### Pipeline

```
部分观测 → Varifold编码 → 潜在空间 → 解码 → 完整形状
```

#### 训练

使用完整的形状作为监督

---

### 17.9 与其他方法的比较

| 方法 | 表示 | 优势 | 劣势 |
|------|------|------|------|
| PointNet | 点特征 | 快速 | 无几何先验 |
| DGCNN | 图特征 | 局部结构 | 需构图 |
| Point Transformer | 注意力 | 长距离依赖 | 计算量大 |
| Neural Varifold | 测度 | 几何一致性 | 实现复杂 |

---

### 17.10 理论分析

#### 表示能力

Neural Varifold可以近似任意紧Varifold

#### 收敛性

在适当条件下，网络输出收敛到真实Varifold

#### 正则化

- 形状先验
- 平滑正则化
- 拓扑约束

---

### 17.11 实验结果

#### 数据集

- ShapeNet
- ModelNet
- 人体扫描数据

#### 指标

| 任务 | 指标 |
|------|------|
| 分类 | Overall Accuracy |
| 分割 | mIoU |
| 补全 | CD (Chamfer Distance) |

#### 性能

在形状匹配和补全任务上达到SOTA

---

### 17.12 未来方向

- 实时处理
- 大规模场景
- 动态形状
- 与生成模型结合

---

### 📊 本讲总结

```
┌─────────────────────────────────────────────────┐
│         Neural Varifolds核心                     │
├─────────────────────────────────────────────────┤
│                                                 │
│   Varifold：几何对象的测度表示                   │
│   μ_S(φ) = ∫_S φ(x, n_x) dH²(x)                │
│                                                 │
│   核度量：                                       │
│   K(x,n,y,m) = k^x(x,y) · k^n(n,m)             │
│                                                 │
│   Neural架构：                                   │
│   点云 → 编码器 → Varifold层 → 表示             │
│                                                 │
│   优势：                                        │
│   • 几何一致性                                  │
│   • 处理不完整数据                              │
│   • 可微分优化                                  │
│                                                 │
│   应用：形状匹配、补全、分类                     │
│                                                 │
└─────────────────────────────────────────────────┘
```

---

### 📚 课后作业

1. **理论题**：推导Varifold度量的计算公式

2. **实现题**：实现高斯核的Varifold内积计算

3. **实验题**：比较不同核函数对形状匹配的影响

4. **扩展题**：设计一个基于Varifold的形状补全网络

---

### 📖 扩展阅读

1. **理论基础**：
   - Charpiat et al., "Varifolds for Shape Matching"
   - Kaltenmark et al., "Kernel Metrics on Normal Distributions"

2. **深度学习**：
   - PointNet系列
   - DeepSDF

3. **Cai相关论文**：
   - Neural Varifolds for 3D shape analysis

---

### 📖 参考文献

1. Charpiat, G., et al. (2017). Varifolds for shape matching. *Geometric Science of Information*.

2. Kaltenmark, I., Charpiat, G., & Charlier, B. (2019). Kernel metrics on normal distributions. *NeurIPS*.

3. Qi, C.R., et al. (2017). PointNet: Deep learning on point sets. *CVPR*.

4. Park, J.J., et al. (2019). DeepSDF: Learning continuous signed distance functions. *CVPR*.
